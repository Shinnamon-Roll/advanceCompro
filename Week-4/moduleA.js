const person = {
    name: 'Shinnamon',
    weight: 56,
    age: 22
}

function sayHello() {
    console.log('Hello there!');
}

function cube(num) {
    return num * num * num;
}

const add = (x, y) => x + y;

const status = true;

module.exports = {
    person,
    sayHello,
    cube,
    add,
    status
};